const playerFactory = (name) => {

}

const gameBoard = (() => {
    const addX = (pos) => 'X';
    const addO = (pos) => 'O'
    return {
        addX,
        addO,
    };
})();

// I actually have no idea what I am doing right now 
// Sleeping might help